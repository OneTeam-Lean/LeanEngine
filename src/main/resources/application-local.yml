spring:
  data:
    mongodb:
      username: root
      password:
      database: test
      authentication-database: admin
management:
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        exclude: env,beans
  endpoint:
    info:
      enabled: true
    health:
      enabled: true
    shutdown:
      enabled: true
