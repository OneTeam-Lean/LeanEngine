spring:
  data:
    mongodb:
      username: admin
      password:
      database: admin
      host:
      port: 9002
management:
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        exclude: env,beans
  endpoint:
    info:
      enabled: true
    health:
      enabled: true
    shutdown:
      enabled: true
