spring:
  datasource:
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/leanengine?setUnicode=true&characterEncoding=utf8
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true